# Phase 0 — 2026-07-31

## What happened
- Rebuilt repo from scratch after diverged-history incident on previous attempt
- Created private GitHub repo, clean single commit, verified push
- Discovered auth was working via cached credential/token, not typed password (unresolved which exactly)
- Built pre-commit hook to grep staged diff for SECRET=/PASSWORD=/API_KEY= patterns
- First hook test looked like it worked but hadn't actually saved (nano didn't persist) - false positive
- Rewrote hook with `cat > file << EOF` instead of nano, verified content with cat before testing
- Second test: hook correctly blocked a staged fake secret (test-secret2.env), confirmed via git status showing it still staged, not committed

## Open question - answer next session
Does .git/hooks/pre-commit travel with the repo when cloned by someone else, or does it
only exist locally? Check: has git status ever shown a path starting with .git/ this session?

## Unresolved
- test-secret2.env still staged locally, unresolved (leave it / unstage it / delete it - decide next time)
- Which auth mechanism actually authenticated pushes tonight - token vs something else
